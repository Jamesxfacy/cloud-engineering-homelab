apiVersion: v1
kind: Service

metadata:
  name: app-web-service
  namespace: development

spec:
  selector:
    app: app-web

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP